from operator import itemgetter     # para importar

ranking = sorted(jogo.items(), key=itemgetter(1))
 dict          (k)    (v)
                0      1
items              0

# coloca o dicionário em ordem pela posição indicada.
